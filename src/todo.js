export default function createTodo(title, description, dueDate, priority, checked){
  return {
                          title,
                          description,
                          dueDate,
    priority,
    checked,
  }
}
